<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    Students List <br>
    <% @students.each do |student| %>
      <p><%= student.name %></p>
      <%= link_to "Delete", student_path(student), method: :delete %>
      <% end %>
      <%= link_to "Create a new student",  %>
  </body>
</html>
